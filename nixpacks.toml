[phases.setup]
nixPkgs = ["nodejs_20", "openssl"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = [
  "npx prisma generate",
  "npx next build"
]

[start]
cmd = "npx prisma migrate deploy && npx next start -p $PORT"
